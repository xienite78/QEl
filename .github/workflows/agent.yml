name: childhood

on:
  push:

jobs:
  ignore:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        persist: ["electricity", "stock", "expression", "concept", "overseas"]
        dense: ["compete", "chat", "providing", "faith"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar rp.jar
